# SSL/TLS 인증서 번들 (HTTPS 요청 시 인증서 검증에 사용)
certifi==2025.10.5

# 문자 인코딩 자동 감지 및 정규화 (웹 스크래핑 시 인코딩 처리)
charset-normalizer==3.4.4

# 등고선(contour) 그리기 라이브러리 (matplotlib의 의존성)
contourpy==1.3.3

# 스타일 사이클링 유틸리티 (matplotlib의 색상/스타일 순환 처리)
cycler==0.12.1

# 함수/클래스에 deprecated 경고를 추가하는 데코레이터 제공
Deprecated==1.3.1

# 폰트 파일 처리 및 조작 라이브러리 (matplotlib의 폰트 처리)
fonttools==4.60.1

# 국제화된 도메인 이름(IDN) 처리 (URL 처리 시 사용)
idna==3.11

# 레이아웃 제약 조건 해결 라이브러리 (matplotlib의 레이아웃 최적화)
kiwisolver==1.4.9

# 데이터 시각화 및 그래프 생성 라이브러리
matplotlib==3.10.7

# 다중 디스패치(multiple dispatch) 지원 (함수 오버로딩)
multipledispatch==1.0.0

# 수치 계산 및 배열 처리 라이브러리 (pandas의 기반)
numpy==2.3.4

# 패키지 버전 및 메타데이터 처리 유틸리티
packaging==25.0

# 데이터 분석 및 조작 라이브러리 (주요 데이터 처리)
pandas==2.3.3

# 이미지 처리 라이브러리 (matplotlib의 이미지 처리)
pillow==12.0.0

# 파서 생성 프레임워크 (matplotlib의 파싱 처리)
pyparsing==3.2.5

# 날짜/시간 처리 확장 라이브러리 (pandas의 날짜 처리)
python-dateutil==2.9.0.post0

# 시간대(timezone) 처리 라이브러리
pytz==2025.2

# HTTP 요청 라이브러리 (웹 스크래핑 및 API 호출)
requests==2.32.5

# Python 2/3 호환성 유틸리티
six==1.17.0

# 시간대 데이터베이스 (pytz의 의존성)
tzdata==2025.2

# HTTP 클라이언트 라이브러리 (requests의 기반)
urllib3==2.5.0

# 데코레이터 및 래퍼 함수 유틸리티
wrapt==2.0.0
